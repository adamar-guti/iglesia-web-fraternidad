ktor:
    deployment:
        port: ${PORT:8080}   # Render asigna el puerto en la variable de entorno
        watch:
            - classes
            - resources
    application:
        modules:
            - com.github.adamarguti.ApplicationKt.module

logging:
    level: INFO



